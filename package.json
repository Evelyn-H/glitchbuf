{
  "name": "glitchbuf",
  "private": true,
  "scripts": {
    "dev": "tsc --noEmit && vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "tone": "^15.5.6"
  }
}
